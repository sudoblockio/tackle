stuff: things

blocker->:
  furry->: confirm Are our friends furry?
  things->: "{{ stuff }}"
  friend: dog
  test_block->:
    if: stuff == 'things'
    my_dog->: "{{friend}}"
    my_public_dog->: "{{blocker.friend}}"
