stuff: things

path_isdir:
  type: path_isdir
  path: a-path

path_exists:
  type: path_exists
  path: a-path/.gitkeep

find_in_parent_dir:
  type: find_in_parent
  target: tests

find_in_parent_file:
  type: find_in_parent
  target: Makefile
