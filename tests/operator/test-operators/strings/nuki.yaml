a_list:
  type: split
  separator: .
  input:
    - stuff.thing
    - things.stuffs

a_str:
  type: split
  separator: .
  input: things.stuffs

join_a_str:
  type: join
  separator: .
  input: "{{ nuki.a_str }}"
