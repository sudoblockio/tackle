_list:
  - stuff
  - things
  - foo
  - bar

appended_list:
  type: append
  input: "{{ nuki._list }}"
  item: donkey


appended_lists:
  type: append
  input: "{{ nuki._list }}"
  item:
    - donkey
    - chickens
