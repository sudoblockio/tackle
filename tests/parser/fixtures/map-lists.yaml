list:
  - 1
  - foo
  - bar

loop->: var {{item}} --for list

hook_list:
  - ->: var list
  - foo:
      ->: var list
  - foo->: var list

hook_list_loops:
  - ->: var {{index}} --for list
  - foo:
      ->: var {{index}} --for list --if 'index != 1'
  - foo->: var {{item}} --for list
