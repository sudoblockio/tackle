stuff: things

stuff_arg->: "{{stuff}}"

stuff_expanded:
  ->: "{{stuff}}"

stuff_expanded_logic:
  ->: "{{stuff}}"
  if: stuff == 'things'

a_map:
  stuff: "{{stuff}}"

a_map_rendered->: "{{ a_map }}"
