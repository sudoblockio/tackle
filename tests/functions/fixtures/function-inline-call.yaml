
do_func->: some_function

some_function<-:
  extends: other_function
  fields:
    full:
      type: str
      description: A description
      default: a-default
    compact: a-default

  args:
    - thing
  exec:
    do->: print "{{thing}}"

  validators:
    thing:

  methods:
    foo:
