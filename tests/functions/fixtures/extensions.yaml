
do_thing:
  <-: function / class
  extends: other_function
  schema:
    full:
      type: bool
      description: A description
      default: a-default
    compact: a-default

  args:
    - thing
  exec:
    do->: print "{{thing}}"

  validators:
    thing:

  methods:
    foo:


do:
  thing->: function
  thing: bar