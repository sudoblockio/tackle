---

full_name: Audrey Roy
project_name: Fake Project Templated
repo_name: "{{ project_name|lower|replace(' ', '-') }}"

other_nuki:
  type: tackle
  template: fake-repo-tmpl-tackle
  no_input: true
  output_dir: .
