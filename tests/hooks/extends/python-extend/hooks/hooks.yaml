DerivedPython<-:
  extends: base_hook
  in_base:
    type: str
    default: bar
  stuff: things


DerivedPythonWithExec<-:
  extends: hook_with_exec
  in_base:
    type: str
    default: bar
  stuff: things
