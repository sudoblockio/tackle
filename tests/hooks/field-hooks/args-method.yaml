a_hook<-:

  method<-:
    literal_compact->: literal foo

    literal_expanded:
      ->: literal foo

    field_default_compact:
      type: str
      default->: literal foo

call->: a_hook method --literal_compact bar --literal_expanded bar --field_default_compact bar