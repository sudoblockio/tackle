
bool->: literal true
int->: literal 1
float->: literal 1.0

# Check same logic in the renderer
bool_render->: "{{bool}}"
int_render->: "{{int}}"
float_render->: "{{float}}"

# Needed special logic to not be converted to int
hex_value->: hex 1
hex_render->: "{{hex(1)}}"
