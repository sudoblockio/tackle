stuff: things

blocker:
  type: block
  items:
    stuff:
      type: print
      statement: here
    things:
      type: print
      statement: "{{ nuki.stuff }}"

yaml:
  type: yaml
  contents: "{{ nuki }}"
  path: output.yaml
