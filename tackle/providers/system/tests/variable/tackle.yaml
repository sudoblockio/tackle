a_list:
  <: var
  input:
    - stuff
    - things

another_list:
  <: var
  input:
    - dogs
    - cats

a_list_remove:
  <: var
  input:
    - _stuff
    - things
    - thangs_
  remove:
    - ^_
    - _$

a_map:
  <: var
  input:
    stuff:
      things: 3
    foo: bar

map_update:
  <: var
  input: "{{ a_map }}"
  update:
    stuff:
      dogs: cats

map_merge:
  <: var
  input: "{{ a_map }}"
  merge_dict:
    stuff:
      dogs: cats

loop_merge:
  <: var
  input: "{{ a_map }}"
  loop: "{{ a_list }}"
  merge_dict:
    stuff:
      "{{ item }}": "{{ index }}"

boolean:
  <: var
  input: true

list_of_lists:
  <: var
  input:
    - [1,2]
    - [3,4]


#print:
#  <: pprint
#  statement: |
#    {% for i in a_list %}{{i}}:
#      animals: "{{ loop.index }}"
#    {% endfor %}

#jinja_merge:
#  <: var
#  input: "{{ a_map }}"
#  merge_dict: |
#    {% for i in a_list %}{{i}}:
#      animals: "{{ loop.index }}"
#    {% endfor %}