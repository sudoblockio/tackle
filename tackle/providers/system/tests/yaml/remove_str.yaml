stuff: things

things:
  <: var
  input:
    - stuff
    - things
    - _stuff
    - _thing
    - _foo
    - _bar

out:
  <: yaml
  contents: "{{ remove_str.things }}"
  path: output.yaml
  remove: "^_"

