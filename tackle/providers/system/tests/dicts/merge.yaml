input_map:
  stuff: blah
  sturf: blu

dict_map:
  stuff: bing
  things: blu

merge_map:
  ->: merge
  inputs:
    - "{{input_map}}"
    - "{{dict_map}}"


#  src: "{{ dict_map }}"
#  input: "{{ input_map }}"

#arg1->: merge {{dict_map}} {{input_map}}