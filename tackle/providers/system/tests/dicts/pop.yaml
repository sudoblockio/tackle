a_list:
  - stuff
  - foo

arg_1->: pop {{a_list}}

a_map:
  stuff: bing
  things: blu
  foo: bar
  baz: grrr


arg_2->: pop {{a_map}} foo

pop_map:
  ->: pop
  src: "{{ a_map }}"
  item: stuff

pop_maps:
  ->: pop
  src: "{{ a_map }}"
  item:
    - stuff
    - things

#output:
#  ->: yaml
#  path: output-pop.yaml
#  contents: "{{ pop }}"
