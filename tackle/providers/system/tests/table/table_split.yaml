column_names:
  ->: var
  input:
    - stuff
    - things

data_split:
  ->: var
  input:
    - foo-bar
    - here-there

table_split:
  ->: table
  column_names: "{{ table_split.column_names }}"
  contents_split: "{{ table_split.data_split }}"
  separator: "-"

data:
  ->: var
  input:
    - ['foo', 'bar']
    - ['here', 1]

print1_:
  ->: print
  input: "{{ this }}"

table:
  ->: table
  column_names: "{{ table_split.column_names }}"
  contents: "{{ table_split.data }}"
