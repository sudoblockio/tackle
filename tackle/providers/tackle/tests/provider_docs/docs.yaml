#this:
#  ->: provider_docs
#  chdir: ".."


render_context:
  ->: provider_docs
  chdir: "../../../rich"

list->: listdir ../../templates

Render provider template:
  ->: generate ../../templates/provider-doc.md index.md
  render_context: "{{render_context}}"
