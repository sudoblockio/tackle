alist:
  - stuff
  - things

blocker->:
  for: alist
  first->: "{{item}}"

#blocker->:
#  for: alist
#  first:
#    ->: tackle
#    chdir: fake-tackle
#
#  print->: print "{{ first }}"
#
#blocker2->:
#  for: alist
#  thing: false
#  print->: print this --if "{{ thing }}"
#
#output->: print "{{ this }}"
