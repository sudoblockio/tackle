inner: innertings

blocker:
  type: block
  items:
    stuff:
      type: print
      statement: here
    things:
      type: print
      statement: "{{ stuff }}"

#yaml:
#  type: pprint
#  statement: "{{ this }}"
