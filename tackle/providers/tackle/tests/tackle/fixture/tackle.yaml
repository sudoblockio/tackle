foo:
  type: var
  input: bar
  when: "{{ foo is not defined }}"

read_stuff:
  type: yaml
  path: example.yaml


var:
  type: var
  input: kjlk
#  statement: "{{ this }}"
#  blah: ldskjl


print:
  type: print
  out: "{{ this }}"
