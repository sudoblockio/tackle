import->:
  - other_hooks/bar.py

FooHook<-:
  bar:
    type: int

FooExtended<-:
  extends: FooModel
  baz: int

BarExtended<-:
  extends: BarModel
  help: Importing `other_hooks` yields a BareModel which we're going to extend here.
  baz: int
