foo:
  - name: bar
    thing: thang
  - name: bar2
    thing: thang2

x->:
  for: i in foo
  do:
    ->: var --merge
    input:
      "{{i.name}}":
        thing->: "{{i.thing}}"

#  merge->:
#    "{{i.name}}":
#      thing->: "{{i.thing}}"

do:
  ->: var --for i in foo
  input:
    "{{i.name}}":
      thing->: "{{i.thing}}"


#  "{{i.name}}":
